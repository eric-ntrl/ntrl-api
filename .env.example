# =============================================================================
# NTRL API Configuration
# =============================================================================

# Database connection string
# Format: postgresql+psycopg2://user:password@host:port/database
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/ntrl_dev

# =============================================================================
# Admin API
# =============================================================================

# Admin API key for pipeline endpoints (optional, no auth if not set)
# ADMIN_API_KEY=your-secret-admin-key

# =============================================================================
# Neutralizer Configuration
# =============================================================================

# Provider to use: 'mock' (default, deterministic) or 'openai' (LLM-based)
NEUTRALIZER_PROVIDER=mock

# OpenAI API Key (required if using openai provider)
# OPENAI_API_KEY=sk-your-api-key-here

# Optional: OpenAI model to use (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# Object Storage (S3)
# =============================================================================

# Storage provider: 's3' (default, production) or 'local' (development)
STORAGE_PROVIDER=local

# S3 Configuration (required if STORAGE_PROVIDER=s3)
# S3_BUCKET=ntrl-raw-content
# S3_REGION=us-east-1

# Optional: Custom S3 endpoint for S3-compatible services (MinIO, DigitalOcean Spaces, etc.)
# S3_ENDPOINT_URL=http://localhost:9000

# Local storage path (used if STORAGE_PROVIDER=local)
LOCAL_STORAGE_PATH=./storage

# Raw content retention period in days (default: 30)
# After this period, raw body content is deleted from S3
# Metadata and neutralized summaries persist indefinitely
RAW_CONTENT_RETENTION_DAYS=30

# =============================================================================
# Logging
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO
